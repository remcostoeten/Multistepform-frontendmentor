const stepOne = document.getElementsByClassName('first-step');
const hideBack = document.getElementsByClassName('btn--back');
for (const i = 0; i < hideBack.length; i++) {
	hideBack.classList.add('aa');
}

for (const i = 0; i < hideBack.length; i++) {
	console.log(hideBack);
}
alert('test');
